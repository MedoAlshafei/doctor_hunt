
import 'package:flutter/material.dart';

class DoctorLiveScreen extends StatelessWidget {
  const DoctorLiveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Doctor Live Screen'),
      ),
    );
  }
}
